
This results in a time complexity of `O(b^d)`, which is **asymptotically the same** as breadth-first search.

### Example Comparison

Let:
- `b = 10` (branching factor)
- `d = 5` (solution depth)

Then:

